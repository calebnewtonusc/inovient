import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Market Research | Inovient Strategy Platform",
  description: "AI marketing SaaS TAM/SAM/SOM analysis — $185B serviceable market growing at 31.4% CAGR through 2030.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
